package environment;
public class SanityCheck {
	public SanityCheck(){
		check();
	}
	
	/* All initial program files and libraries etc...that need to be loaded and/or 
	 * checked will be checked here. Return false if something fails.
	 */
	private boolean check(){
		return true;
	}
}